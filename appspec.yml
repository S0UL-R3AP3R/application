version: 0.0
os: linux
image: timeoff
files:
  - source: /
    destination: /
    overwrite: true
permissions: 
  - object: /
    pattern: "*.sh"
    owner: root
    group: root
    mode: 755
    type:
     - file
hooks:
  ApplicationStart:
    -location: /deploy.sh timeoff:latest
    timeout: 120


